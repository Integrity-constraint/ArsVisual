<Window x:Class="ArsVisual.Resources.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArsVisual.Resources"
        mc:Ignorable="d"
        Title="ColorPickerWindow" Height="250" Width="460"
        WindowStartupLocation="CenterScreen" AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize"
         WindowStyle="None">
    <Window.Resources>
        <!-- Стиль для TextBox -->
        <Style TargetType="TextBox" x:Key="ColorValueTextBox">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <!-- Стиль для слайдеров -->
        <Style TargetType="Slider" x:Key="ColorSlider">
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="255"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="Black" BorderThickness="0.5" Background="White" CornerRadius="15" >
        
    
    <Grid Background="Transparent" MouseLeftButtonDown="Drag" >


        <TextBlock Margin="10,0,0,0"
              Text="Мастер настроек" 
             
              VerticalAlignment="Top" />



        <Grid Background="Transparent" Margin="16,20,20,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="77"/>
                <RowDefinition Height="Auto" MinHeight="42.96"/>
            </Grid.RowDefinitions>


            <!-- Alpha (прозрачность) -->

            <TextBlock Text="A:" Grid.Column="0" VerticalAlignment="Center" Grid.ColumnSpan="2" Height="16" Margin="0,0,348,0"/>
            <Slider x:Name="AlphaSlider" Grid.Column="1" Style="{StaticResource ColorSlider}" Height="18" Margin="5,0,5,0"/>
            <TextBox x:Name="AlphaTextBox" Grid.Row="0" Grid.Column="2" Style="{StaticResource ColorValueTextBox}" Margin="5,0,0,0"/>

           
            <TextBlock Text="R:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Foreground="Red" Grid.ColumnSpan="2" Height="16" Margin="0,0,348,0"/>
            <Slider x:Name="RedSlider" Grid.Row="1" Grid.Column="1" Style="{StaticResource ColorSlider}" Height="18" Margin="5,0,5,0"/>
            <TextBox x:Name="RedTextBox" Grid.Row="1" Grid.Column="2" Style="{StaticResource ColorValueTextBox}" Margin="5,0,0,0"/>

            
            <TextBlock Text="G:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Foreground="Green" Grid.ColumnSpan="2" Height="16" Margin="0,0,348,0"/>
            <Slider x:Name="GreenSlider" Grid.Row="2" Grid.Column="1" Style="{StaticResource ColorSlider}" Height="18" Margin="5,0,5,0"/>
            <TextBox x:Name="GreenTextBox" Grid.Row="2" Grid.Column="2" Style="{StaticResource ColorValueTextBox}" Margin="5,0,0,0"/>

      
            <TextBlock Text="B:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Foreground="Blue" Grid.ColumnSpan="2" Height="16" Margin="0,0,348,0"/>
            <Slider x:Name="BlueSlider" Grid.Row="3" Grid.Column="1" Style="{StaticResource ColorSlider}" Height="18" Margin="5,0,5,0"/>
            <TextBox x:Name="BlueTextBox" Grid.Row="3" Grid.Column="2" Style="{StaticResource ColorValueTextBox}" Margin="5,0,0,0"/>

           
            <Border Name="PreviewBorder" Grid.Row="5" Grid.ColumnSpan="3" CornerRadius="5" BorderBrush="#FFAAAAAA" BorderThickness="1"
    Background="{Binding ElementName=colorPickerWindow, Path=PreviewColorBrush}" Margin="0,0,0,10">
                <TextBlock x:Name="PreviewText" Text="Предпросмотр" HorizontalAlignment="Center" VerticalAlignment="Center"
           FontWeight="Bold" FontSize="16"/>
            </Border>

          
            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10" Grid.Column="1" Width="270">
                <TextBox x:Name="HexColorTextBox" Width="100" Margin="0,0,10,0" TextAlignment="Center"/>
                <Button x:Name="AcceptButton" Style="{StaticResource ToolBarButtonBaseStyle}" Content="OK" Width="70" Margin="5,0" Click="AcceptButton_Click"/>
                <Button x:Name="CancelButton" Style="{StaticResource ToolBarButtonBaseStyle}" Content="Отмена" Width="70" Margin="5,0" Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
    </Border>

</Window>
