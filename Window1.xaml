<Window x:Class="ArsVisual.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:m="clr-namespace:ArsVisual"
        xmlns:c="clr-namespace:ArsVisual.Controls"
        WindowStartupLocation="CenterScreen"
        Title="ArsVisual"
        Height="850" Width="1000" WindowState="Maximized" Icon="/icons/logo.png" WindowStyle="None" Background="#FF37383A">

    <Window.Resources>
        <ContextMenu x:Key="DesignerCanvasContextMenu">
            <MenuItem Header="Paste" Command="{x:Static ApplicationCommands.Paste}">
                <MenuItem.Icon>
                    <Image Source="Resources/Images/Paste.png" Width="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Select All" Command="{x:Static m:DesignerCanvas.SelectAll}"/>
        </ContextMenu>
        <Style x:Key="TextButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
           
            <Setter Property="Cursor" Value="Hand"/>
           
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

  

    <Grid MouseDown="Drag" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

     
        <Grid Grid.Row="0" Background="Transparent" Cursor="">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition  Height="*"/>
                <RowDefinition  Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock  Grid.Column="0" Text="ArsVisual" 
                   VerticalAlignment="Center" Margin="10,0,0,0"
                   Foreground="White" FontSize="14"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Cursor="Hand">
                <Button Width="40" Cursor="Hand" Click="Minimize"
                    Content="🗕" ToolTip="Свернуть"
                  />
                <Button  Width="40"  Cursor="Hand"  Click="Open"
                    Content="🗖" ToolTip="Развернуть"
                  />
                <Button  Width="40"  Click="Clostw" Cursor="Hand"  Name="btnshut"
                    Content="✕" ToolTip="Закрыть"
                  />
            </StackPanel>
            <Button Grid.Row="1" Click="OpenPopup" Margin="10,0,0,0" HorizontalAlignment="Left">Popup</Button>
            <Popup x:Name="MyPopup" Placement="Bottom" StaysOpen="False">
                <Border  Background="#333333" BorderBrush="Black" BorderThickness="1" 
                    Padding="1" CornerRadius="10">
                    <StackPanel >
                        <Button Content="Открыть проект" Command="{x:Static ApplicationCommands.Open}"
 CommandTarget="{Binding ElementName=MyDesignerCanvas}" Style="{StaticResource TextButtonStyle}"/>
                        <Button Content="Новый файл" Command="{x:Static ApplicationCommands.New}"
 CommandTarget="{Binding ElementName=MyDesignerCanvas}" Style="{StaticResource TextButtonStyle}"/>
                        <Button Content="Сохранить проект" Command="{x:Static ApplicationCommands.Save}"
CommandTarget="{Binding ElementName=MyDesignerCanvas}" Style="{StaticResource TextButtonStyle}"/>
                        <Button Content="Настройки" Command="{x:Static m:DesignerCanvas.Settings}"
 CommandTarget="{Binding ElementName=MyDesignerCanvas}" Style="{StaticResource TextButtonStyle}"/>
                        <Button Content="Экспорт" Command="{x:Static m:DesignerCanvas.SaveTOpng}"
CommandTarget="{Binding ElementName=MyDesignerCanvas}" Style="{StaticResource TextButtonStyle}"/>
                        <Button Content="Печать" Command="{x:Static ApplicationCommands.Print}"
CommandTarget="{Binding ElementName=MyDesignerCanvas}" Style="{StaticResource TextButtonStyle}"/>

                    </StackPanel>
                </Border>
            </Popup>
        </Grid>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="239*"/>
                <ColumnDefinition Width="6*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ContentControl Content="{StaticResource MyToolbar}" Grid.ColumnSpan="2"/>

            <Grid Grid.Row="1" Margin="0,10,0,0" Grid.ColumnSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="265"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
             
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <Expander Background="#1B1B1B" Header="Простая Блок-схема" Content="{StaticResource FlowChartStencils}" IsExpanded="True"/>
                        <Expander Background="#1B1B1B" Header="Объекты Active Directory" Content="{StaticResource NetStencils}" IsExpanded="True"/>
                        <Expander Background="#1B1B1B" Header="Сценарий выполнения UML" Content="{StaticResource UMLPosl}" IsExpanded="True"/>
                        <Expander Background="#1B1B1B" Header="Описание рабочих областей Офисов/комнат" Content="{StaticResource RoomStencils}" IsExpanded="True"/>
                    </StackPanel>
                </ScrollViewer>
              
                <GridSplitter Focusable="False" Width="2" Background="LightGray"
                    VerticalAlignment="Stretch" HorizontalAlignment="Right"/>

                <GroupBox Grid.Column="1">
                    <TabControl x:Name="MainTabControl" SelectionChanged="PageSwitch">
                       
                        <TabItem Header="Страница 1">
                            <TabItem.HeaderTemplate>
                                <DataTemplate>
                                    <DockPanel>
                                        <TextBlock Text="Страница 1"/>
                                        <Button Background="Transparent" BorderBrush="Transparent" Content="×" Click="CloseTabClick" 
                                            Width="20" Height="20" Padding="0"
                                            DockPanel.Dock="Right"
                                            Tag="{Binding RelativeSource={RelativeSource AncestorType=TabItem}}"/>
                                    </DockPanel>
                                </DataTemplate>
                            </TabItem.HeaderTemplate>
                            <Grid>
                                <ScrollViewer Name="DesignerScrollViewer"
                    Background="Transparent"
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Auto">
                                    <m:DesignerCanvas x:Name="MyDesignerCanvas"
                         MinHeight="800"
                         MinWidth="1000"
                         AllowDrop="True"
                         Background="White">
                                    </m:DesignerCanvas>
                                </ScrollViewer>
                                <m:ZoomBox Width="180"
             HorizontalAlignment="Right"
             VerticalAlignment="Top"
             ScrollViewer="{Binding ElementName=DesignerScrollViewer}"
             Margin="0,10,30,0"/>
                            </Grid>
                        </TabItem>

                       
                        <TabItem Background="White" x:Name="AddTabButton" Header="+">
                            <TabItem.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="+" FontSize="16" FontWeight="Bold"/>
                                </DataTemplate>
                            </TabItem.HeaderTemplate>
                        </TabItem>
                    </TabControl>
                </GroupBox>
            </Grid>
        </Grid>
    </Grid>
</Window>
